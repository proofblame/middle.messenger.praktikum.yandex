<div class="info__row">
  {{#if info}}
    <span class="info__row-label">{{item}}</span>
    <span class="info__row-data">{{info}}</span>
  {{else}}
    <button class="info__row-button {{className}}">{{item}}</button>
  {{/if}}
</div>
